# FP-005 单机运行配置模板
# 复制为 .env 使用（cp .env.example .env），或直接用环境变量覆盖（环境变量优先）。
# 仅支持以下三项——单环境、单租户，无多环境/多租户开关。

# 监听地址：本机访问用 127.0.0.1；单台云机/容器需对外提供访问时改为 0.0.0.0
HOST=127.0.0.1

# 监听端口
PORT=3000

# 数据目录（FP-001 持久化使用；启动时自动创建；相对路径相对仓库根解析）
DATA_DIR=./data
